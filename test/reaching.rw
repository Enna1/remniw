// RUN: %remniw %s -o %t ; echo 1 | lli %t | FileCheck %s
// RUN: %remniw %s -o %t -debug ; echo 1 | lli %t | FileCheck %s

func main() int {
   var x,y,z int;
   x = input;
   while (x>1) {
      y = x/2;
      if (y>3){
         x = x-y;
      }
      z = x-4;
      if (z>0){
         x = x/2;
      }
      z = z-1;
   }
   output x;
   return 0;
}

// CHECK: 1
